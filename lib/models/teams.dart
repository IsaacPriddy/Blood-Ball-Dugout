class TeamsModel {
  // Data types
  final String name;
  final String tier;
  // final String image; // Possibly image?

  // Constructor
  TeamsModel({
    required this.name,
    required this.tier,
  });
}